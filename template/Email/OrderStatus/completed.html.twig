<!doctype html>
<html lang="ru">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    </head>
    <body>
        <main class="container">
            <div class="bg-light p-3 rounded mt-3">
                <p class="lead text-center"><strong>Заказ №{{ number }} готов</strong></p>
            </div>
            <div class="p-1 rounded">
                <p class="lead"><h3><strong>Состав заказа:</strong></h3></p>
            </div>
            <div class="row" style="width: 85%; margin: 0 auto;">
                {% for item in basket %}
                <div class="col-4 pt-3">
                    <div class="card" style="width: 18rem;">
                        <img src="{{ item['imgSrc'] }}" class="card-img-top" alt="{{ item['imgAlt'] }}">
                        <div class="card-body">
                            <h5 class="card-title">{{ item['title'] }}</h5>
                            <p class="card-text">{{ item['description'] }}</p>
                            <p class="card-text">Автор(ы): {{ item['author'] }}</p>
                            <div class="row">
                                    <div class="col-6" style="text-align: left;">
                                        {{ item['quantity'] }} шт.
                                    </div>
                                    <div class="col-6" style="text-align: right;">
                                        <strong>{{ item['price'] }} ₽</strong>
                                    </div>
                            </div>
                            <a class="btn btn-primary mt-2" href="{{ download }}" role="button">Скачать</a>
                        </div>
                      </div>
                </div>
                {% endfor %}
            </div>
            <div class="bg-light p-1 rounded mt-2">
                <p class="lead"><h3>Общая стоимость: {{ totalPrice }}</h3></p>
            </div>
        </main>
        <footer class="my-5 pt-5 text-muted text-center text-small">
            <p class="mb-1">&copy; 2022-{{ year }} <a href="{{ siteUrl }}">{{storeName}}</a></p>
        </footer>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    </body>
</html>
